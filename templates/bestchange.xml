<?xml version="1.0"?>
<rates>
    {% for rate in exchange_rates %}
    <item>
        <from>{{ rate.currency_left.name_for_best_change }}</from>
        <to>{{ rate.currency_right.name_for_best_change }}</to>
        <in>{{ rate.value_left }}</in>
        <out>{{ rate.value_right }}</out>
        <amount>572962.42</amount>
        <minamount>{{ rate.min_value }}</minamount>
        <maxamount>{{ rate.max_value }}</maxamount>
    </item>
    {% endfor %}
</rates>
